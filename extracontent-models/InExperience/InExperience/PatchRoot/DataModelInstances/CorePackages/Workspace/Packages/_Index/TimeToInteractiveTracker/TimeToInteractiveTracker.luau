local v0 = require(script.TimeToInteractiveTracker);
local v1 = require(script.Stores.types);
return {
    TimeToInteractiveTracker = v0, 
    Types = v1, 
    ["jest.config"] = script["jest.config"]
};