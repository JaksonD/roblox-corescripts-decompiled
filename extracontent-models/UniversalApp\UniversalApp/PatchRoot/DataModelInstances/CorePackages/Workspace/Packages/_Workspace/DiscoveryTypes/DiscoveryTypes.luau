local _ = require(script.RoduxTypes);
local v1 = require(script.SearchLandingPageSurface);
local v2 = require(script.PlayabilityStatusEnum);
local v3 = require(script.PlayableUxTreatmentEnum);
local _ = require(script.EnumGameDetailErrorTypes);
local v5 = require(script.EnumIgrsRating);
local v6 = require(script.GameCollectionItemTypes);
local v7 = require(script.Flags.GetFIntLuaAppRecommendedGamesCount);
local l_FFlagEnableTopSongsSort_0 = require(script.Parent.SharedFlags).FFlagEnableTopSongsSort;
local l_FFlagLuaAppUnifyPlayButtonMobileEdp_0 = require(script.Parent.SharedFlags).FFlagLuaAppUnifyPlayButtonMobileEdp;
local l_FFlagLuaAppShowNoticeOnEdp_0 = require(script.Parent.SharedFlags).FFlagLuaAppShowNoticeOnEdp;
local l_GetFFlagUIBloxUseFoundationButton_0 = require(script.Parent.SharedFlags).UIBlox.GetFFlagUIBloxUseFoundationButton;
local l_FFlagSearchOmniAutocompletePage_0 = require(script.Parent.SharedFlags).FFlagSearchOmniAutocompletePage;
local l_Parent_0 = script.Parent;
return {
    OmniFeedPropsKey = require(l_Parent_0.AppCommonLib).Symbol.named("OmniFeedProps"), 
    Constants = {
        SORT_INDEX_SELECTION_ORDER_MULTIPLIER = 100, 
        SORT_SELECTION_ORDER_BASE = -1000, 
        DEFAULT_GAME_FETCH_COUNT = 40, 
        GAME_CAROUSEL_CHILD_PADDING = 12, 
        GAME_CAROUSEL_PADDING = 15, 
        GAME_CAROUSEL_SIDE_COMPACT_PADDING = 24, 
        GAME_CAROUSEL_SIDE_WIDE_PADDING = 48, 
        GAME_DETAILS_ACTION_BAR_HEIGHT = (not not l_FFlagLuaAppUnifyPlayButtonMobileEdp_0 or l_GetFFlagUIBloxUseFoundationButton_0()) and 50 or 48, 
        GAME_GRID_CHILD_PADDING = 12, 
        GAME_GRID_PADDING = 15, 
        GAME_GRID_TILE_FOOTER_HEIGHT = 70, 
        MAX_RECOMMENDED_GAMES_FETCH_COUNT = v7()
    }, 
    DataSourceType = {
        DiscoveryApi = "DiscoveryApi", 
        ExploreApi = "ExploreApi", 
        GamesApi = "GamesApi", 
        Local = "Local", 
        OmniAutocompleteApi = l_FFlagSearchOmniAutocompletePage_0 and "OmniAutocompleteApi" or nil, 
        SearchApi = "SearchApi", 
        Unknown = "Unknown"
    }, 
    ExperienceDetailsNoticeType = if l_FFlagLuaAppShowNoticeOnEdp_0 then {
        InExperiencePurchase = "InExperiencePurchase"
    } else nil, 
    ExploreContentType = require(script.ExploreContentType), 
    GameBasicStatsLayoutType = require(script.GameBasicStatsLayoutType), 
    GameCardLayoutType = require(script.GameCardLayoutType), 
    EnumGameDetailErrorTypes = require(script.EnumGameDetailErrorTypes), 
    EnumIgrsRating = v5, 
    GameMediaImageType = require(script.GameMediaImageType), 
    GameSortGroups = require(script.GameSortGroups), 
    GameTopicIds = require(script.GameTopicIds), 
    OmniRecommendationsContentType = require(script.OmniRecommendationsContentType), 
    OmniFeedTypes = require(script.OmniFeedTypes), 
    OmniFeedConstants = require(script.OmniFeedConstants), 
    PlayabilityStatusEnum = v2, 
    PlayableUxTreatmentEnum = v3, 
    SearchSourceEvent = require(script.SearchSourceEvent), 
    SearchTextTopicIds = require(script.SearchTextTopicIds), 
    SearchLandingPageSurface = v1, 
    SortContentType = if l_FFlagEnableTopSongsSort_0 then require(script.SortContentType) else nil, 
    GameCollectionItemTypes = v6, 
    ["jest.config"] = script["jest.config"]
};