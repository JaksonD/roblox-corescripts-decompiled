local v0 = require(script.useProperty);
local v1 = require(script.useSelected);
local v2 = require(script.useGuiState);
local v3 = require(script.useHoverState);
local v4 = require(script.usePressState);
return {
    useProperty = v0, 
    useSelected = v1, 
    useGuiState = v2, 
    useHoverState = v3, 
    usePressState = v4
};