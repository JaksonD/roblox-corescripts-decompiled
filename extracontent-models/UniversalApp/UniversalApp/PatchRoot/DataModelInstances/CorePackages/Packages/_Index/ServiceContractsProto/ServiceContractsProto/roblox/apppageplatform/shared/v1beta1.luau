require(script.Parent.Parent.Parent.Parent.proto)

local action_bar = require(script.action_bar)
local actions = require(script.actions)
local age_recommendation_data = require(script.age_recommendation_data)
local analytics_data = require(script.analytics_data)
local associated_items_row = require(script.associated_items_row)
local attribution_row = require(script.attribution_row)
local badge = require(script.badge)
local badge_data = require(script.badge_data)
local badge_tile = require(script.badge_tile)
local button = require(script.button)
local capability = require(script.capability)
local card = require(script.card)
local catalog_category_menu = require(script.catalog_category_menu)
local catalog_item_carousel = require(script.catalog_item_carousel)
local catalog_item_group = require(script.catalog_item_group)
local catalog_sort_data = require(script.catalog_sort_data)
local catalog_sort_prop_types = require(script.catalog_sort_prop_types)
local catalog_virtualized_feed = require(script.catalog_virtualized_feed)
local chip = require(script.chip)
local circle_icon = require(script.circle_icon)
local client_capabilities = require(script.client_capabilities)
local coachmark = require(script.coachmark)
local collection_carousel = require(script.collection_carousel)
local collection_grid = require(script.collection_grid)
local component_shared = require(script.component_shared)
local content_rating_label = require(script.content_rating_label)
local content_row = require(script.content_row)
local countdown_text_icon_row = require(script.countdown_text_icon_row)
local creator_data = require(script.creator_data)
local details_page_image_header = require(script.details_page_image_header)
local developer_product_data = require(script.developer_product_data)
local dialog = require(script.dialog)
local display_location = require(script.display_location)
local divider = require(script.divider)
local dual_action_chip = require(script.dual_action_chip)
local event_data = require(script.event_data)
local event_tile = require(script.event_tile)
local expandable_text = require(script.expandable_text)
local filter_pills_carousel = require(script.filter_pills_carousel)
local focus_nav_actions = require(script.focus_nav_actions)
local fragment = require(script.fragment)
local game_pass_data = require(script.game_pass_data)
local game_tile = require(script.game_tile)
local game_tile_active_friends_footer = require(script.game_tile_active_friends_footer)
local hero_unit = require(script.hero_unit)
local hold_to_play = require(script.hold_to_play)
local hydration_content = require(script.hydration_content)
local hydration_content_type = require(script.hydration_content_type)
local hydration_data_spec = require(script.hydration_data_spec)
local icon = require(script.icon)
local image = require(script.image)
local info_table = require(script.info_table)
local info_table_cell = require(script.info_table_cell)
local link_text = require(script.link_text)
local marketplace_asset_data = require(script.marketplace_asset_data)
local marketplace_bundle_data = require(script.marketplace_bundle_data)
local marketplace_catalog_category_data = require(script.marketplace_catalog_category_data)
local marketplace_look_data = require(script.marketplace_look_data)
local media_asset_data = require(script.media_asset_data)
local media_gallery = require(script.media_gallery)
local overflow_menu = require(script.overflow_menu)
local page_entry = require(script.page_entry)
local page_entry_content = require(script.page_entry_content)
local play_button = require(script.play_button)
local play_button_contextual_message = require(script.play_button_contextual_message)
local playability_data = require(script.playability_data)
local prop_condition = require(script.prop_condition)
local prop_types = require(script.prop_types)
local prop_types_engine = require(script.prop_types_engine)
local section_header = require(script.section_header)
local see_all_tile = require(script.see_all_tile)
local share_link_data = require(script.share_link_data)
local sheet_full_bleed = require(script.sheet_full_bleed)
local single_item_collection = require(script.single_item_collection)
local slot_overlay = require(script.slot_overlay)
local social_link_data = require(script.social_link_data)
local song_data = require(script.song_data)
local song_tile = require(script.song_tile)
local subscription_cell = require(script.subscription_cell)
local subscription_data = require(script.subscription_data)
local tabs = require(script.tabs)
local template_entry = require(script.template_entry)
local text = require(script.text)
local text_icon_row = require(script.text_icon_row)
local text_pill = require(script.text_pill)
local tile = require(script.tile)
local tile_footer = require(script.tile_footer)
local ui_component_schema = require(script.ui_component_schema)
local ui_component_type = require(script.ui_component_type)
local universe_data = require(script.universe_data)
local universe_user_favorite_data = require(script.universe_user_favorite_data)
local universe_user_follow_data = require(script.universe_user_follow_data)
local universe_user_vote_data = require(script.universe_user_vote_data)
local user_data = require(script.user_data)
local vertical_feed = require(script.vertical_feed)
local view = require(script.view)

return {
	action_bar = action_bar,
	actions = actions,
	age_recommendation_data = age_recommendation_data,
	analytics_data = analytics_data,
	associated_items_row = associated_items_row,
	attribution_row = attribution_row,
	badge = badge,
	badge_data = badge_data,
	badge_tile = badge_tile,
	button = button,
	capability = capability,
	card = card,
	catalog_category_menu = catalog_category_menu,
	catalog_item_carousel = catalog_item_carousel,
	catalog_item_group = catalog_item_group,
	catalog_sort_data = catalog_sort_data,
	catalog_sort_prop_types = catalog_sort_prop_types,
	catalog_virtualized_feed = catalog_virtualized_feed,
	chip = chip,
	circle_icon = circle_icon,
	client_capabilities = client_capabilities,
	coachmark = coachmark,
	collection_carousel = collection_carousel,
	collection_grid = collection_grid,
	component_shared = component_shared,
	content_rating_label = content_rating_label,
	content_row = content_row,
	countdown_text_icon_row = countdown_text_icon_row,
	creator_data = creator_data,
	details_page_image_header = details_page_image_header,
	developer_product_data = developer_product_data,
	dialog = dialog,
	display_location = display_location,
	divider = divider,
	dual_action_chip = dual_action_chip,
	event_data = event_data,
	event_tile = event_tile,
	expandable_text = expandable_text,
	filter_pills_carousel = filter_pills_carousel,
	focus_nav_actions = focus_nav_actions,
	fragment = fragment,
	game_pass_data = game_pass_data,
	game_tile = game_tile,
	game_tile_active_friends_footer = game_tile_active_friends_footer,
	hero_unit = hero_unit,
	hold_to_play = hold_to_play,
	hydration_content = hydration_content,
	hydration_content_type = hydration_content_type,
	hydration_data_spec = hydration_data_spec,
	icon = icon,
	image = image,
	info_table = info_table,
	info_table_cell = info_table_cell,
	link_text = link_text,
	marketplace_asset_data = marketplace_asset_data,
	marketplace_bundle_data = marketplace_bundle_data,
	marketplace_catalog_category_data = marketplace_catalog_category_data,
	marketplace_look_data = marketplace_look_data,
	media_asset_data = media_asset_data,
	media_gallery = media_gallery,
	overflow_menu = overflow_menu,
	page_entry = page_entry,
	page_entry_content = page_entry_content,
	play_button = play_button,
	play_button_contextual_message = play_button_contextual_message,
	playability_data = playability_data,
	prop_condition = prop_condition,
	prop_types = prop_types,
	prop_types_engine = prop_types_engine,
	section_header = section_header,
	see_all_tile = see_all_tile,
	share_link_data = share_link_data,
	sheet_full_bleed = sheet_full_bleed,
	single_item_collection = single_item_collection,
	slot_overlay = slot_overlay,
	social_link_data = social_link_data,
	song_data = song_data,
	song_tile = song_tile,
	subscription_cell = subscription_cell,
	subscription_data = subscription_data,
	tabs = tabs,
	template_entry = template_entry,
	text = text,
	text_icon_row = text_icon_row,
	text_pill = text_pill,
	tile = tile,
	tile_footer = tile_footer,
	ui_component_schema = ui_component_schema,
	ui_component_type = ui_component_type,
	universe_data = universe_data,
	universe_user_favorite_data = universe_user_favorite_data,
	universe_user_follow_data = universe_user_follow_data,
	universe_user_vote_data = universe_user_vote_data,
	user_data = user_data,
	vertical_feed = vertical_feed,
	view = view
}
